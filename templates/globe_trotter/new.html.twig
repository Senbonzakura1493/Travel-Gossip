{% extends 'base.html.twig' %}


{% form_theme form 'bootstrap_4_layout.html.twig' %}
{% block body%}
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <a class="navbar-brand" href="/">Travel & Gossip</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="/traveling">Les voyages <span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/new">Ajouter un voyage</a>
                </li> 
                <li class="nav-item">
                    <a class="nav-link" href="/newCategory">Ajouter une catégorie de voyage</a>
                </li> 
                <li class="nav-item">
                    <a class="nav-link" href="/Categories">Les categories</a>
                </li>   
        </div>
    </nav>
    <img src="https://cdn-images-1.medium.com/max/1200/1*kfoZJqoU5IDWz-u7ZclDtQ.jpeg" width = 1550px height =400px></img>
    <div class="jumbotron">

    {% if editMode %}
    <h2>Modification fiche voyage </h2>
    {% else %}
    <h2>Nouvelle fiche critique</h2>
    {% endif %}
    
    {{form_start(form)}}

        {{form_widget(form)}}

        <button type="submit" class="btn btn-primary">
        {% if editMode %}
            Modifier
        {% else %}
            Soumettre
        </button>
        {% endif %}

    {{form_end(form)}} 
    </div>

{% endblock %}